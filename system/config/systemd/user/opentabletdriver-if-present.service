[Unit]
Description=Start OpenTabletDriver if the configured tablet is already plugged in
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=oneshot
ExecStart=%h/.local/bin/otd-usb-autostart start-if-present

[Install]
WantedBy=graphical-session.target
