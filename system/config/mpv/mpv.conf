# HDR / wide-gamut playback on this HDR desktop.
# Without target-colorspace-hint, mpv often picks sRGB and PQ / HDR files
# look washed (HDR-as-SDR). Useful for GSR captures and other HDR media.
vo=gpu-next
hwdec=auto
target-colorspace-hint=yes
